<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.proyectofinal_android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Activities.ActivityDetallesPedido"></activity>
        <activity android:name=".Activities.ActivityCarrito" />
        <activity android:name=".Activities.ActivityProducto" />
        <activity
            android:name=".Activities.ActivityListaProductos"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".Activities.ActivityRegistro" />
        <activity
            android:name=".Activities.ActivityLogin"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>